{{!-- Sử dụng layout product.hbs --}}
{{!< layout/product}}

<section class="hero-fruit-bg py-5">
    <div class="container">
        <h2 class="text-center text-white mb-4">
            <span class="fruit-icon">🍎</span> Sản Phẩm Nổi Bật <span class="fruit-icon">🍊</span>
        </h2>
        <div id="featuredCarousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                {{#each featuredProducts}}
                <div class="carousel-item {{#if @first}}active{{/if}}">
                    <div class="row justify-content-center">
                        <div class="col-md-4 col-sm-6 mb-4">
                            <div class="product-card position-relative">
                                <span class="fresh-badge">Mới</span>
                                {{#if discount}}
                                <span class="discount-badge">{{discount}}% OFF</span>
                                {{/if}}
                                <img src="{{this.imageURL.[0].url}}" class="product-image" alt="{{name}}" style="max-width: 200px; height: auto; object-fit: cover; margin: 0 auto; display: block;">
                                <div class="p-4">
                                    <h3 class="product-title">{{this.name}}</h3>
                                    <div class="rating">
                                        {{#times rating}}
                                        <i class="fas fa-star"></i>
                                        {{/times}}
                                        {{#times (math 5 '-' rating)}}
                                        <i class="far fa-star"></i>
                                        {{/times}}
                                    </div>
                                    <div class="d-flex align-items-center mb-2">
                                        <span class="product-price">{{formatMoney price}}₫</span>
                                        {{#if oldPrice}}
                                        <span class="product-old-price ms-2">{{formatMoney oldPrice}}₫</span>
                                        {{/if}}
                                        {{#if isOrganic}}
                                        <span class="organic-badge">Hữu cơ</span>
                                        {{/if}}
                                    </div>
                                    <p class="text-light mb-3">{{sortDescription}}</p>
                                    <div class="d-flex gap-2">
                                        <button class="btn btn-primary" onclick="addToCart('{{id}}', '{{name}}')">
                                            <i class="fas fa-shopping-cart me-1"></i> Thêm vào giỏ
                                        </button>
                                        <a href="/products/{{id}}" class="btn btn-fruit-orange">
                                            <i class="fas fa-eye me-1"></i> Xem chi tiết
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#featuredCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#featuredCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>
</section>
