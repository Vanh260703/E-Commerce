<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if pageTitle}}{{pageTitle}}{{else}}Cửa hàng trái cây tươi{{/if}}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #22c55e;
            --secondary-color: #f0fdf4;
            --accent-color: #fb923c;
            --text-dark: #1f2937;
            --text-light: #6b7280;
            --fruit-red: #ef4444;
            --fruit-yellow: #fbbf24;
            --fruit-purple: #a855f7;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 100%);
            color: var(--text-dark);
        }

        .navbar {
            background: linear-gradient(135deg, var(--primary-color), #16a34a);
            box-shadow: 0 4px 15px rgba(34, 197, 94, 0.2);
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
        }

        .user-avatar {
            width: 32px; /* chỉnh size tùy ý */
            height: 32px;
            object-fit: cover;
            border-radius: 50%; /* bo tròn ảnh */
        }

        .product-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
            overflow: hidden;
            height: 100%;
            border: 2px solid transparent;
        }

        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 25px 50px rgba(34, 197, 94, 0.15);
            border-color: var(--primary-color);
        }

        .product-image {
            width: 100%;
            height: 250px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .product-card:hover .product-image {
            transform: scale(1.1);
        }

        .product-title {
            font-weight: 600;
            color: var(--text-dark);
            font-size: 1.1rem;
            margin-bottom: 10px;
        }

        .product-price {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .product-old-price {
            text-decoration: line-through;
            color: var(--text-light);
            font-size: 1rem;
        }

        .discount-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: linear-gradient(135deg, var(--fruit-red), #dc2626);
            color: white;
            padding: 8px 12px;
            border-radius: 25px;
            font-size: 0.8rem;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(239, 68, 68, 0.3);
        }

        .fresh-badge {
            position: absolute;
            top: 15px;
            left: 15px;
            background: linear-gradient(135deg, var(--primary-color), #16a34a);
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.7rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color), #16a34a);
            border: none;
            border-radius: 25px;
            padding: 12px 25px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #16a34a, #15803d);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(34, 197, 94, 0.3);
        }

        .btn-fruit-orange {
            background: linear-gradient(135deg, var(--accent-color), #ea580c);
            border: none;
            color: white;
        }

        .btn-fruit-orange:hover {
            background: linear-gradient(135deg, #ea580c, #dc2626);
            color: white;
        }

        .category-filter {
            background: white;
            border-radius: 20px;
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            border: 2px solid #ecfdf5;
        }

        .rating {
            color: var(--fruit-yellow);
            margin-bottom: 10px;
        }

        .footer {
            background: linear-gradient(135deg, var(--text-dark), #111827);
            color: white;
            padding: 50px 0;
            margin-top: 60px;
        }

        .hero-fruit-bg {
            background: linear-gradient(135deg, var(--primary-color), #16a34a);
            position: relative;
            overflow: hidden;
        }

        .hero-fruit-bg::before {
            content: '🍎🍊🍌🍇🥝🍓';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            font-size: 3rem;
            opacity: 0.1;
            display: flex;
            align-items: center;
            justify-content: center;
            letter-spacing: 2rem;
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-10px) rotate(2deg); }
        }

        .fruit-icon {
            display: inline-block;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        .organic-badge {
            background: linear-gradient(135deg, #22c55e, #16a34a);
            color: white;
            padding: 3px 8px;
            border-radius: 10px;
            font-size: 0.7rem;
            font-weight: 600;
            text-transform: uppercase;
            margin-left: 5px;
        }

        .vitamin-badge {
            background: linear-gradient(135deg, var(--fruit-yellow), #f59e0b);
            color: white;
            padding: 2px 6px;
            border-radius: 8px;
            font-size: 0.6rem;
            font-weight: 600;
            margin: 2px;
        }
    </style>

    {{#if additionalCSS}}
        <link rel="stylesheet" href="{{additionalCSS}}">
    {{/if}}
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <span class="fruit-icon">🍎</span>
                {{#if storeName}}{{storeName}}{{else}}Fresh Fruit Store{{/if}}
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/products">🏠 Trang chủ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products/all-products">🍇 Trái cây</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/categories">📂 Danh mục</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">📞 Liên hệ</a>
                    </li>
                </ul>
                
                <div class="navbar-nav">
                    <a class="nav-link position-relative" href="/cart">
                        <i class="fas fa-shopping-cart"></i>
                        <span id="cart-count-badge" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="display: none;">
                            0
                        </span>
                    </a>
                    {{#if user}}
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{user.avatar}}" alt="Avatar" class="user-avatar">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                            <li><a class="dropdown-item" href="/user/profile">Trang cá nhân</a></li>
                            <li><a class="dropdown-item" href="/user/orders">Đơn hàng của tôi</a></li>
                            <li><a class="dropdown-item" href="/user/settings">Cài đặt</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="#" id="logoutBtn">
                                    Đăng xuất
                                </a>
                            </li>
                        </ul>
                    </div>
                    {{else}}
                    <a class="nav-link" href="/auth/login">
                        <i class="fas fa-user"></i>
                    </a>
                    {{/if}}
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        {{{body}}}
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-md-6 mb-4">
                    <h5>{{#if storeName}}{{storeName}}{{else}}Fresh Fruit Store{{/if}}</h5>
                    <p class="text-light">🍎 Cửa hàng trái cây tươi ngon, chất lượng cao được nhập khẩu trực tiếp và trái cây Việt Nam an toàn.</p>
                </div>
                <div class="col-lg-2 col-md-6 mb-4">
                    <h6>Liên kết</h6>
                    <ul class="list-unstyled">
                        <li><a href="/" class="text-light text-decoration-none">🏠 Trang chủ</a></li>
                        <li><a href="/products" class="text-light text-decoration-none">🍇 Trái cây</a></li>
                        <li><a href="/about" class="text-light text-decoration-none">ℹ️ Về chúng tôi</a></li>
                        <li><a href="/contact" class="text-light text-decoration-none">📞 Liên hệ</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-md-6 mb-4">
                    <h6>Hỗ trợ khách hàng</h6>
                    <ul class="list-unstyled">
                        <li><a href="/shipping" class="text-light text-decoration-none">🚚 Chính sách giao hàng</a></li>
                        <li><a href="/returns" class="text-light text-decoration-none">🔄 Đổi trả</a></li>
                        <li><a href="/fresh-guarantee" class="text-light text-decoration-none">✅ Cam kết tươi ngon</a></li>
                        <li><a href="/faq" class="text-light text-decoration-none">❓ FAQ</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-md-6 mb-4">
                    <h6>Liên hệ</h6>
                    <p class="text-light">
                        <i class="fas fa-phone me-2"></i>0123 456 789<br>
                        <i class="fas fa-envelope me-2"></i>info@shop.com<br>
                        <i class="fas fa-map-marker-alt me-2"></i>Hà Nội, Việt Nam
                    </p>
                </div>
            </div>
            <hr class="my-4">
            <div class="row">
                <div class="col-md-6">
                    <p class="text-light mb-0">&copy; 2024 {{#if storeName}}{{storeName}}{{else}}Fresh Fruit Store{{/if}}. 🍎 Trái cây tươi ngon mỗi ngày.</p>
                </div>
                <div class="col-md-6 text-end">
                    <a href="#" class="text-light me-3"><i class="fab fa-facebook"></i></a>
                    <a href="#" class="text-light me-3"><i class="fab fa-instagram"></i></a>
                    <a href="#" class="text-light me-3"><i class="fab fa-twitter"></i></a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    {{#if additionalJS}}
        <script src="{{additionalJS}}"></script>
    {{/if}}
    <script>

    function showToast(type, message) {
        const Toast = Swal.mixin({
            toast: true,
            position: 'top-end',
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true,
        });
        Toast.fire({
            icon: type,
            title: message
        });
    }

    async function updateCartCount() {
        try {
            const res = await fetch('/api/cart', {
                method: 'GET',
                credentials: 'include', // gửi cookie (accessToken)
            });

            const data = await res.json();

            if (!data.success || !Array.isArray(data.cartItems)) {
                console.warn('❌ Không lấy được giỏ hàng');
                return;
            }

            const cartCount = data.cartItems.reduce((total, item) => total + item.quantity, 0);
            const badge = document.getElementById('cart-count-badge');

            if (badge) {
                badge.textContent = cartCount;
                badge.style.display = cartCount > 0 ? 'inline-block' : 'none';
            }
        } catch (err) {
            console.error('Lỗi khi lấy giỏ hàng:', err);
        }
    }

    document.getElementById('logoutBtn').addEventListener('click', async (e) => {
    e.preventDefault();

    const res = await fetch('/auth/logout', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
    });

    if (res.ok) {
        window.location.href = '/auth/login';
    } else {
        alert('Đăng xuất thất bại');
    }
});

        // Smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

    </script>
</body>
</html>